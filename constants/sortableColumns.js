const SORT_BY_VALUES = ['asc', 'desc'];

const TESTS_SORTABLE_COLUMNS = [
  'name',
  'status',
  'visibility',
  'duration',
  'createdAt',
];

module.exports = { TESTS_SORTABLE_COLUMNS, SORT_BY_VALUES };
